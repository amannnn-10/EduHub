import React from 'react';
import ReactDOM from 'react-dom';
import LoginPage from './LoginPage'; // Adjust the file path based on your project structure

ReactDOM.render(<LoginPage />, document.getElementById('root'));
